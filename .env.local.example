# Copy to .env.local and point at your FastAPI backend.
# If unset, the app falls back to built-in demo data (src/lib/mock-data.ts)
# so every screen still renders before the backend exists.
NEXT_PUBLIC_API_URL=http://localhost:8000
